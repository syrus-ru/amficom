kisserver_v1/README

This module requires the following external files present to be built
successfully:
	o general.idl from idl_v1 -- put to $(IDLDIR) (defaults to idl/);
	o util.jar from util -- put to $(LIBDIR) (defaults to lib/ or ../lib,
	  depending on local settings);
	o corba.jar from idl_v1 -- put to $(LIBDIR) (defaults to lib/ or ../lib,
	  depending on local settings);
	o servermisc.jar from servermisc_v1 -- put to $(LIBDIR) (defaults to
	  lib/ or ../lib, depending on local settings).
