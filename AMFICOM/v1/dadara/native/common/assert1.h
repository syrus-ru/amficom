#ifndef _assert_h
#define _assert_h

#include <stdlib.h>
#include <assert.h>
/*
#define assert(x) \
{ if(x) ; else { \
fprintf(stderr, "Assertion failed: file %s line %d condition %s\n",__FILE__, __LINE__, #x); \
abort(); exit(3); } }
*/
#endif

