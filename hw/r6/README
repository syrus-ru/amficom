rc
 Скрипт, запускающий заглушку в обычном режиме - приём запросов на измерение через именованный канал, проведение измерений и отправка результатов в именованный канал. Библиотеки от Nettest лежат на reseacrh в директории /export/lib/hw/nettest

stub.jar
 Эта библиотека должна быть на РТУ в classpath для всех процессов впереди других библиотек. Она переопределяет некоторые неттестовские классы, чтобы обеспечит работу заглушки с РТУ. См директорию D:\QuestProbe\run на РТУ, файлы moduleServer.bat, probeServer.bat.

setup.sql
 Этот SQL-скрипт нужно выполнить на БД РТУ с вновь установленным ПО. Он добавляет к таблице запросов на измерения один столбец, необходимый для работы с Амфиком. Соединение с БД РТУ: websrvlt/questfbr@qprobe

rsetup
 Скрипт, запускающий заглушку в режиме настройки. Настройка по умолчанию: один оптический переключатель с двумя волокнами, на 1-м и 2-м портах.

delalarms.sql
 Вспомогательный SQL-скрипт, с помощью которого можно сбросить все сигналы тревоги на РТУ. Внимание! Эти сигналы тревоги не имеют ничего общего с сигналами в системе Амфиком; это чисто неттестовская фигня. Нормальному человеку они вообще мешать не должны.

